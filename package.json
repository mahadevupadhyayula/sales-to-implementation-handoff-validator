{
  "name": "sales-to-implementation-handoff-validator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "validate:fixtures": "node --conditions=react-server --import tsx scripts/validate-fixtures.ts",
    "test": "vitest run",
    "test:e2e": "echo 'Phase 2: browser tests are not implemented yet'",
    "eval": "node --conditions=react-server --import tsx evals/run-evals.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "server-only": "^0.0.1",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
