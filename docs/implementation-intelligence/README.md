# Implementation Intelligence Source Documents

The Phase 0 build request names these source documents:

- `DEMO_WORKFLOW_CONTEXT.md`
- `IMPLEMENTATION_INTELLIGENCE_WORKFLOW.md`
- `implementation-intelligence-proof-brief.md`

They were not present in the repository or available elsewhere in the execution environment when Phase 0 was performed, so their contents could not be read, copied, or safely reconstructed. Place authoritative copies in this directory before Phase 1. Do not replace them with inferred content.

The repository-specific operating constraints derived from the confirmed build plan are in the root `AGENTS.md`; the intended implementation footprint is in `BUILD_MAP.md`.
